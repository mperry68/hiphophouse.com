# Cloudflare Pages Redirects
# This file handles URL rewrites and redirects

# Redirect trailing slashes to non-trailing (optional)
/*/ /$1 301

# Handle .html extension removal (optional - uncomment if you want clean URLs)
# /classes/* /classes/$1.html 200
# /about /about.html 200
# /schedule /schedule.html 200
# /gallery /gallery.html 200
# /contact /contact.html 200

# 404 fallback - serve index.html for unknown routes
/* /index.html 404

